# University
University enrollment management. Based on the rolling project from Monash University FIT9131 unit.

## Commands
- `make build` compile all Java sources
- `make test` run assertion-based tests
- `make run` start the console program
